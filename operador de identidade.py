saldo = 1000
limite = 500

# "is" é usado para verifica se ocupão a mesma região de memória retorno sera false
#verifica se le é
print(saldo is limite)
# "is not" é usado para verifica se realmente não ocupão a mesma região de memória  retorno sera true
#verifica se le não é
print(saldo is not limite)